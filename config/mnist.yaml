seed_everything: null

ckpt_path: null

data:
  class_path: "contrastive.MNISTDataModule"
  init_args:
    data_dir: "run/data"
    mean: null
    std: null
    batch_size: 32
    num_workers: 0

model:
  class_path: "contrastive.ConvEmbedding"
  init_args:
    num_channels: [32, 64]
    num_features: 2
    margin: 1.0
    mine_mode: "batch_all"
    squared: true
    eps: 1e-06
    lr: 0.001

trainer:
  accelerator: "cpu"
  max_epochs: 50
  log_every_n_steps: 100
  logger:
    class_path: "lightning.pytorch.loggers.TensorBoardLogger"
    init_args:
      save_dir: "run/"
      name: "mnist"
      version: null
  callbacks:
    - class_path: "lightning.pytorch.callbacks.ModelCheckpoint"
      init_args:
        filename: "best"
        monitor: "val_loss"
        mode: "min"
        save_top_k: 1
    - class_path: "lightning.pytorch.callbacks.ModelCheckpoint"
      init_args:
        filename: "{epoch}"
        save_top_k: -1
        every_n_epochs: 2
        save_last: true
    # - class_path: "lightning.pytorch.callbacks.ModelCheckpoint"
    #   init_args:
    #     filename: "{step}"
    #     save_top_k: -1
    #     every_n_train_steps: 200
    #     save_last: false

